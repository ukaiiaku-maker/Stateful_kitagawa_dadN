# v2.8.1 first commands

Apply the hotfix to the persistent registry, then rerun the existing failure gate with the same OUTROOT. The completed physical result is discovered by `sigma_a_MPa` metadata and reused; the solver is not launched again.
